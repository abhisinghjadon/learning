// Program 48: Double every number in an array using .map().
let arr = [1, 5, 9, 4, 10, 8, 12];
// .map() loops through the original array element by element.
let doubledNumbers = arr.map((num) => num * 2);
console.log(doubledNumbers);
