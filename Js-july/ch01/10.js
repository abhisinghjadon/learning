//Program 2: Take two numbers and print their sum.
const prompt = require("prompt-sync")();
const num1 = Number(prompt("Enter the First Number : "));
const num2 = Number(prompt("Enter the Second Number : "));

console.log(num1 + num2);
