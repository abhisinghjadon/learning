/*
Program 102: Query Selectors & NodeLists
Task: Select a single element using document.querySelector() and multiple elements using document.querySelectorAll(). Log the resulting NodeList to the console.
Syntax Focus: document.querySelector(), document.querySelectorAll(), NodeList.forEach().
 */

const tab = document.querySelector("#list");
console.log(tab);

const tab = document.querySelectorAll(".listData");
console.log(tab);
